import { Link } from 'react-router-dom'; import type { Project } from '../data/projects';
export default function ProjectCard({project,index=0}:{project:Project,index?:number}){return <Link className="project-card" data-cursor-view to={`/projects/${project.slug}`} data-reveal><div className="image-reveal" data-image-reveal><img src={project.hero} alt={`${project.title} architectural visualization`} loading={index<2?'eager':'lazy'} /></div><div className="project-card-meta"><span>{String(index+1).padStart(2,'0')}</span><h3>{project.title}</h3><span>{project.location} · {project.year}</span></div></Link>}
