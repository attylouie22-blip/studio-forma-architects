import { useRef } from 'react'; import { gsap } from 'gsap'; import { ScrollTrigger } from 'gsap/ScrollTrigger'; import { useGSAP } from '@gsap/react'; import { projects } from '../data/projects'; import { Link } from 'react-router-dom'; import { useReducedMotion } from '../hooks/useReducedMotion'; gsap.registerPlugin(ScrollTrigger,useGSAP);
export default function HorizontalProjects(){const ref=useRef<HTMLElement>(null); const reduced=useReducedMotion(); useGSAP(()=>{if(reduced||window.matchMedia('(max-width: 900px)').matches||!ref.current)return; const track=ref.current.querySelector<HTMLElement>('.horizontal-track'); if(!track)return; const getX=()=>-(track.scrollWidth-innerWidth); const tween=gsap.to(track,{x:getX,ease:'none',scrollTrigger:{trigger:ref.current,start:'top top',end:()=>`+=${track.scrollWidth-innerWidth}`,pin:true,scrub:1,invalidateOnRefresh:true}}); return()=>{tween.kill()};},{scope:ref,dependencies:[reduced]}); return <section className="horizontal-section" ref={ref}><div className="section-heading horizontal-heading"><p className="eyebrow">Selected Work</p><h2>Projects move with the pace of your scroll.</h2></div><div className="horizontal-track">{projects.map((p,i)=><Link to={`/projects/${p.slug}`} className="horizontal-card" data-cursor-view key={p.slug}><img src={p.hero} alt={p.title} loading="lazy"/><div><span>{String(i+1).padStart(2,'0')}</span><h3>{p.title}</h3><p>{p.location} · {p.year} · {p.category}</p></div></Link>)}</div></section>}
