import { useRef } from 'react'; import { services } from '../data/services'; import ProcessStory from '../sections/ProcessStory'; import { useScrollReveal } from '../hooks/useScrollReveal';
import { usePageMeta } from '../hooks/usePageMeta';
export default function Services(){usePageMeta('Services','Architectural design, residential design, commercial architecture, interior design, renovation, planning, consultation, and 3D visualization.');const ref=useRef<HTMLDivElement>(null);useScrollReveal(ref);return <div className="page" ref={ref}><header className="page-hero"><p className="eyebrow">Services</p><h1>Integrated design services, scaled to the project.</h1></header><section className="services-page-list">{services.map(([name,desc],i)=><article key={name} data-reveal><span>{String(i+1).padStart(2,'0')}</span><h2>{name}</h2><p>{desc}</p></article>)}</section><ProcessStory/></div>}