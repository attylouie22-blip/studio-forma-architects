import { useRef } from 'react'; import { Link } from 'react-router-dom'; import Hero from '../sections/Hero'; import PinnedStory from '../sections/PinnedStory'; import HorizontalProjects from '../sections/HorizontalProjects'; import ProcessStory from '../sections/ProcessStory'; import Testimonials from '../sections/Testimonials'; import { services } from '../data/services'; import { siteConfig } from '../data/siteConfig'; import { useScrollReveal } from '../hooks/useScrollReveal'; import { useParallax } from '../hooks/useParallax';
import { usePageMeta } from '../hooks/usePageMeta';
export default function Home(){usePageMeta('Home','Studio Forma Architects creates refined residential, commercial, hospitality, renovation, and interior architecture in the Philippines.');const ref=useRef<HTMLDivElement>(null); useScrollReveal(ref); useParallax(ref); return <div ref={ref}><Hero/><section className="intro"><p className="eyebrow" data-reveal>Architecture / Interiors / Place</p><div><h2 data-reveal>{siteConfig.secondaryTagline}</h2><p data-reveal>{siteConfig.about}</p></div></section><PinnedStory/><section className="philosophy"><div className="philosophy-number">01</div><div><p className="eyebrow">Design Philosophy</p><h2 data-reveal>Quiet architecture can still make a powerful first impression.</h2><p data-reveal>We use proportion, sequence, climate, and material honesty to create spaces that feel inevitable rather than over-designed.</p></div></section><HorizontalProjects/><section className="services-preview"><div className="section-heading"><p className="eyebrow">Services</p><h2 data-reveal>From first idea to finished place.</h2></div><div className="service-grid">{services.slice(0,4).map(([name,desc],i)=><article data-reveal key={name}><span>0{i+1}</span><h3>{name}</h3><p>{desc}</p></article>)}</div><Link className="text-link" to="/services">Explore all services ↗</Link></section><ProcessStory/><section className="about-preview"><div className="about-portrait" data-parallax="-8"><div className="portrait-mark">SF</div></div><div><p className="eyebrow">Studio</p><h2 data-reveal>Designed with rigor. Experienced with ease.</h2><p data-reveal>Studio Forma treats every commission as both a technical problem and a human experience. We value clarity, listening, and buildings that grow better with time.</p><Link className="text-link" to="/about">About the studio ↗</Link></div></section><Testimonials/><section className="cta"><p className="eyebrow">New Commission</p><h2>Tell us about your project.</h2><Link className="button dark" to="/contact">Start a Project</Link></section></div>}